<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.familyservicestoronto.shortcut">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shortcut">
        <activity android:name=".AddAppMenu" />
        <activity android:name=".YoutubeTutorialActivity" />
        <activity android:name=".TutorialActivity" />
        <activity android:name=".TutorialPageActivity" />
        <activity android:name=".FacebookTutorialActivity" />
        <activity android:name=".GmailTutorialActivity" />
        <activity android:name=".ZoomTutorialActivity" />
        <activity android:name=".GoogleSearchActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WhatsappTutActivity" />
    </application>

</manifest>